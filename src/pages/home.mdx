
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';

---
title: Astro Page
---

# Astro Page

This is a page for Jon Christie's Astro Page



## This is a heading

This is a paragraph

## This is another heading

This is another paragraph

## This is a table

| Name | Age |
| ---- | --- |
| Jon  | 30  |
| Bob  | 40  |

## This is a list

- Item 1
- Item 2
- Item 3


## This is a code block

```js
const foo = 'bar';
```

## This is a code block with a title

```js:title=src/pages/home.mdx

const foo = 'bar';
```

## This is a code block with a title and a line highlight

```js:title=src/pages/home.mdx#L1-L3

const foo = 'bar';
```

## This is bar graph in code written in typescript

```typescript

import { Bar } from 'react-chartjs-2';
import { ChartData } from 'chart.js';
import { useState } from 'react';
import { useQuery } from 'react-query';

const data: ChartData = {
  labels: ['Red', 'Blue', 'Yellow', 'Green', 'Purple', 'Orange'],
  datasets: [
    {
      label: '# of Votes',
      data: [12, 19, 3, 5, 2, 3],
      backgroundColor: [
        'rgba(255, 99, 132, 0.2)',
        'rgba(54, 162, 235, 0.2)',
        'rgba(255, 206, 86, 0.2)',
        'rgba(75, 192, 192, 0.2)',
        'rgba(153, 102, 255, 0.2)',
        'rgba(255, 159, 64, 0.2)',
      ],
      borderColor: [
        'rgba(255, 99, 132, 1)',
        'rgba(54, 162, 235, 1)',
        'rgba(255, 206, 86, 1)',
        'rgba(75, 192, 192, 1)',
        'rgba(153, 102, 255, 1)',
        'rgba(255, 159, 64, 1)',
      ],
      borderWidth: 1,
    },
  ],
};

const options = {
  scales: {
    y: {
      beginAtZero: true,
    },
  },
};

const BarChart = () => {
  const [chartData, setChartData] = useState<ChartData>(data);

  return (
    <div>
      <Bar data={chartData} options={options} />
    </div>
  );
};

export default BarChart;

```

